import React from 'react'


interface ReviewCardProps {
    imgSrc: string;
    name: string;
    desc: string;
    classes?: string;
}
const ReviewCard = ({ imgSrc, name, desc, classes }: ReviewCardProps) => {
  return (
    <div>
      
    </div>
  )
}

export default ReviewCard
