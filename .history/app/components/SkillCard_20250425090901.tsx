import React from 'react'


interface SkillCardProps {
    imagSrc: string;
    label: string;
    desc: string;
    classes?: string;
}
const SkillCard = ({  }: SkillCardProps) => {
  return (
    <div>
      
    </div>
  )
}

export default SkillCard
